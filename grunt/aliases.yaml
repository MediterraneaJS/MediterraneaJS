default:
    - 'build'

build:
    - 'clean'
    - 'assemble'
    - 'copy' # resources,fonts,...
    - 'less'
    - 'concat'
    - 'newer:imagemin'
    - 'autoprefixer'

minimize:
    - 'cssmin'
    - 'uglify'

serve:
    - 'build'
    - 'connect'
    - 'watch'

serve-dist:
    - 'clean'
    - 'build'
    - 'minimize'
    - 'connect:livereload:keepalive'

publish:
    - 'clean'
    - 'build'
    - 'minimize'
    - 'gh-pages'

